version: '2'

services:
 tunnel:
  image: registry.cn-hangzhou.aliyuncs.com/zanecloud/tunneld:0.1.0
  restart: always
  network_mode: host
  pid: host
  environment:
   - DOCKER_HOST=tcp://${LOCAL_HOST}:2375
  labels:
   - com.zanecloud.omega.labels.system=true
  command: --debug true
yu
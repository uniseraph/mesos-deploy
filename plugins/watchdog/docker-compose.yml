version: '2'

services:
 watchdog:
  image: registry.cn-hangzhou.aliyuncs.com/omega-reg/watchdog:0.1
  restart: always
  network_mode: host
  pid: host
  labels:
   - com.github.omega.labels.system=true
  command: manage --host ${LOCAL_IP}:2375 ${DIS_URL}